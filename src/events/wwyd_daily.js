const {
  Events,
  EmbedBuilder,
  MessageFlags,
  AttachmentBuilder,
} = require("discord.js");

module.exports = {
  name: "WWYD_Daily",
  once: false,
  async execute(client) {
    console.log("wwyd daily event");
  },
};
